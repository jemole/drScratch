{% load i18n %}

{% block content %}
<div class="container">
    <div class="col-md-7 col-lg-offset-2">
        <section id="dashboard">
            <div class="container-fluid">
                <div class="row marketing">
                    <div class="jumbotron">
                        <h4> {% trans "New organization in Dr.Scratch!"%}</h4>
                        <p>  {% trans "You're receiving this email because you have successfully sign up in Dr.Scratch for organizations."%}</p>
                        {% block reset_link %}
                        {% endblock %}
                        <p> {% trans "Thanks for using our website!"%}</p>
                        <p> {% trans "The Dr.Scratch team"%}
                    </div>
                </div>
            </div>
        </section>
    </div>
</div>
{% endblock %}
